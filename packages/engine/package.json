{
  "name": "@living-journeys/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure TypeScript engine: virtual-time clock, journey evaluation, content schema. No React, no DOM, no Supabase.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
